#include "main.h"
/**
 * main - entry point
 *
 * print_alphabet_x10 - prints all alphabets in lowercase 10 times.
 *
 * Return: On success 1
 *
 */

void print_alphabet_x10(void)
{
	char p, k;

	for (k = 0; k < 10; k++)
	{

		for (p = 'a'; p <= 'z'; i++)
		{
			_putchar(p);
		}
		_putchar('\n');
	}
}
